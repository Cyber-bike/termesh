{
  "name": "termy-a0-spike",
  "private": true,
  "description": "Standalone A0 spike scripts (verifies @number0/iroh loads inside Obsidian's Electron renderer). Not part of the plugin build; run `npm install` inside this directory only.",
  "dependencies": {
    "@number0/iroh": "1.1.0"
  }
}
